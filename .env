# Frontend (Vite) — required for browser → API calls
VITE_API_URL=http://localhost:5000/api

# Backend API (also read when you run `npm run dev:backend` from repo root; cwd picks up this file)
PORT=5000
CLIENT_URL=http://localhost:5173

# Atlas: include /database before ? — use your chosen DB name (must exist or Mongo creates collections on first write)
MONGODB_URI=mongodb+srv://db_user:NFbZZEgvLtsWDH3q@cluster0.u1ywo7b.mongodb.net/vulnguard?retryWrites=true&w=majority&appName=Cluster0
